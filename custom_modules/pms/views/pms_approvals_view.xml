<odoo>
        <!-- Tree View -->
        <record id="view_pms_approvals_tree" model="ir.ui.view">
            <field name="name">pms.approval.tree</field>
            <field name="model">pms.approval</field>
            <field name="arch" type="xml">
                <list editable="top" create="false">
                    <!-- Define your tree view fields here -->
                    <field name="name"/>
                    <field name="date"/>
                    <field name="final_date"/>
                    <field name="description"/>
                    <field name="status"/>
                    <field name="source"/>
                </list>
            </field>
        </record>

        <!-- Server Action -->
        <record id="action_server_pms_approve" model="ir.actions.server">
            <field name="name">Approve</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="pms.model_pms_approval"/>
            <field name="binding_model_id" ref="pms.model_pms_approval"/>
            <field name="state">code</field>
            <field name="code">
        if records:
            records.action_approve()
            </field>
        </record>

        <record id="action_server_pms_deny" model="ir.actions.server">
            <field name="name">Deny</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="pms.model_pms_approval"/>
            <field name="binding_model_id" ref="pms.model_pms_approval"/>
            <field name="state">code</field>
            <field name="code">
        if records:
            records.action_deny()
            </field>
        </record>

        
          <!-- Action -->
        <record id="action_pms_approvals" model="ir.actions.act_window">
            <field name="name">PMS Approval</field>
            <field name="res_model">pms.approval</field>
            <field name="view_mode">list</field>
        </record>
</odoo>