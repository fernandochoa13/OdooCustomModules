<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Commission Calculation Wizard Form View -->
        <record id="view_commission_calculation_wizard_form" model="ir.ui.view">
            <field name="name">commission.calculation.wizard.form</field>
            <field name="model">commission.calculation.wizard</field>
            <field name="arch" type="xml">
                <form string="Commission Calculation Wizard">
                    <group>
                        <group>
                            <field name="commission_rate" widget="float"/>
                            <field name="quickbooks_file" filename="quickbooks_filename" widget="binary"/>
                            <field name="quickbooks_filename" invisible="1"/>
                        </group>
                    </group>
                    <group string="Output Files">
                        <group>
                            <field name="quickbooks_output_file" filename="quickbooks_output_filename" widget="binary" readonly="1"/>
                            <field name="quickbooks_output_filename" invisible="1"/>
                        </group>
                        <group>
                            <field name="odoo_output_file" filename="odoo_output_filename" widget="binary" readonly="1"/>
                            <field name="odoo_output_filename" invisible="1"/>
                        </group>
                    </group>
                    <footer>
                        <button name="action_process_file" string="Process File" type="object" class="btn-primary" 
                               />
                        <button string="Close" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Commission Calculation Wizard Action -->
        <record id="action_commission_calculation_wizard" model="ir.actions.act_window">
            <field name="name">Commission Calculation</field>
            <field name="res_model">commission.calculation.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>

